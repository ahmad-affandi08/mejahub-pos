<IfModule mod_rewrite.c>
	RewriteEngine On

    # Biarkan request ke /.well-known tetap lewat (SSL/verification)
    RewriteRule ^\.well-known/ - [L]

    # Hindari loop jika sudah berada di /public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]

</IfModule>
